# 08 — 并发安全 + 集成测试套件

**What to build:** 解决 SQLite 并发写入 `database is locked` 风险——启用 WAL 模式并添加文件级写锁；新增并发事务测试、Provider 降级链集成测试、多协程同时 commit 不出现数据损坏或脏读。

**Blocked by:** 07 — Provider 观测层（Provider 降级集成测试依赖观测层落位）

**Status:** ✅ done

## Acceptance criteria

- [x] SQLite engine 初始化时启用 WAL 模式（`PRAGMA journal_mode=WAL`）
- [x] 文件级写入锁 `FileLock` 实现：同一时间仅一个进程可执行写事务
- [x] 并发测试：两个进程同时对不同 novel 执行写操作 → 无 `database is locked` 异常，数据完整
- [x] Provider 降级集成测试：Provider A 超时 → 自动降级到 Provider B → 返回正确结果并记录 stats（依赖 T07）
- [x] 并发事务隔离性测试：事务 A 未提交时，事务 B 读取到的数据版本正确
- [x] 287 项测试 + 新增的并发测试全部通过
