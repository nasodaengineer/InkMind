# 09 — 工程基础设施 + 边缘修复

**What to build:** 修复审查报告中的 M4/M6/N1–N5 问题——`--json` 参数解析健壮化、引入软删除机制代替物理删除、添加 `.editorconfig`、`pre-commit` hooks、测试别名精简、ORM 时间默认值、`version` 命令输出真实版本号。

**Blocked by:** None — can start immediately.

**Status:** ✅ done

## Acceptance criteria

- [x] `--json` 标志解析修复：`--title "--json"` 正确解析，支持 `--json=true` / `--json false` 语法
- [x] 软删除机制：所有实体（Novel/Chapter/Character/World）添加 `deleted_at` 字段，`delete()` 改为标记删除；查询默认过滤已删除记录
- [x] `.editorconfig` 文件添加到项目根目录（缩进 4 空格、UTF-8、LF 换行）
- [x] `pyproject.toml` 添加 `line-length = 120`
- [x] `pre-commit` 配置文件（hooks：ruff check、ruff format、mypy、pytest）
- [x] 测试中 `from sqlalchemy import select as sa_select` 等冗余别名精简
- [x] ORM 表 `created_at` / `updated_at` 添加 `default=func.now()`
- [x] `inkmind version` 输出真实语义版本号 `0.1.0`
- [x] 287 项测试全部通过
